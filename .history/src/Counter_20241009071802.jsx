import { useState } from "react";


function ToDoCounter() {

     const [count, setCount] = useState({
          count: '0';
     })

       handleTodoCount() {
    this.setState((state) => ({
      ...state,
      count: state.count + 1,
    }));

     return (
          <>
          
          
          
          </>
     )
}